import React from 'react';
import GoogleApiWrapper, { MapContainer } from "../components/Map/index";

const ResultsMap = (props) => {return (
            <GoogleApiWrapper
                name1="Test Prop Marker"
            />
    );
};

export default ResultsMap;